import java.util.*;

import javax.sound.sampled.SourceDataLine;
public class pracString1 {

    public static void main(String[] args) {
     Scanner sc=new Scanner(System.in);
     //ques 1: Take an aray of Strings input from the user and find the cumulative(combined)length of all those strings.
    //   System.out.println("enter the size of the String array: ");
    // int size=sc.nextInt();
    // int length=0;
    //  String str[]=new String[size];
    //  System.out.println("Enter the elements of th array : ");
    //  for(int i=0;i<size;i++) {
    //      str[i]=sc.nextLine();
    //      length=length + str[i].length();
    //  }
    //  System.out.println(length);
     
        // 2nd IMP question : input a string from the user.Create a new string called the letter 'e' in the original string with letter 'i'.
        //Example : original ="eabcdef" ; result ="iabcdif"
        // System.out.println("enter the String :");
        // String oG=sc.nextLine();
        // String result="" ;
        // for(int i=0;i<oG.length();i++){ 
        //     if(oG.charAt(i)== 'e'){
        //         result= result +"i"; 
        //     }
        //      else{ 
        //          result= result +  oG.charAt(i);
        //  }
        // }
        // System.out.println(result);

        //3rd question : INput an email from the user. YOU have to create a username from the email by deleting the part that comes after '@'.DIsplay that username to the user
//         System.out.println("enter the email Id : ");
//         String iD=sc.next();
//         String name="";
//         for(int i=0;i<iD.length();i++){ 
//             if(iD.charAt(i)=='@'){ 
//                 break;
//             }
//             else{ 
//                 name=name + iD.charAt(i);
//             }
//         }
//         System.out.println(name);
// }

       //question 4 : reverse the string it is in string builder

       


}
}